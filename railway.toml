[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn codegreen.wsgi --workers 2 --bind 0.0.0.0:$PORT"
preDeployCommand = "python manage.py migrate --run-syncdb && python manage.py collectstatic --noinput && python manage.py setup_oauth && python manage.py load_ndvi"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
